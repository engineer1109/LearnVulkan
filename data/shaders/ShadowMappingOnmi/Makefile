all:
	glslangValidator -V scene.glsl.frag -o scene.so.frag
	glslangValidator -V scene.glsl.vert -o scene.so.vert
	glslangValidator -V room.glsl.frag -o room.so.frag
	glslangValidator -V room.glsl.vert -o room.so.vert
	glslangValidator -V offscreen.glsl.frag -o offscreen.so.frag
	glslangValidator -V offscreen.glsl.vert -o offscreen.so.vert
	glslangValidator -V cubemapdisplay.glsl.frag -o cubemapdisplay.so.frag
	glslangValidator -V cubemapdisplay.glsl.vert -o cubemapdisplay.so.vert

